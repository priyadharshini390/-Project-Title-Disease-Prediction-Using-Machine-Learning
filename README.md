# -Project-Title-Disease-Prediction-Using-Machine-Learning
from sklearn.model_selection import train_tes# Step 9: Display result if prediction[0] == 1:     print("\nResult: The person is likely to have Diabetes.") else:     print("\nResult: The person is nott_split from sklearn.linear_model import LogisticRegression from sklearn.metrics import accuracy_score import pandas as pd
